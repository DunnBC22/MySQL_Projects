CREATE TABLE Independence100 (
	ranking INT NOT NULL,
    restaurant varchar(45),
    sales INT,
    averageCheck INT,
    city varchar(15),
    state varchar(6), 
    meals_served INT,
    PRIMARY KEY (ranking)
)